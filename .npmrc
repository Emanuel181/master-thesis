# Allow installing packages with peer dependency conflicts
# Required because next-auth@5.0.0-beta.30 has optional peerDep on @simplewebauthn/browser@^9.0.1
# but we use @simplewebauthn/browser@13.x which is compatible
legacy-peer-deps=true
